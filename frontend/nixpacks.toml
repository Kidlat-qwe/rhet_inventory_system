# Coolify / Nixpacks — Vite SPA
# Build with Vite 6 (Rollup). Avoid Vite 8/Rolldown native bindings on Nixpacks.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
