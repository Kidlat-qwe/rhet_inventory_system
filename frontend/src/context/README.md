# Context

React context providers shared across the authenticated app shell.

| File | Purpose |
|------|---------|
| `SettingsContext.jsx` | Org settings from `GET /settings` (threshold, couriers, sizes, shirt logos, branding, Help Assistant). Use `useSettings()`. |
